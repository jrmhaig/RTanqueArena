<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to "RTanque Arena", root_path, class: "navbar-brand" %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <% if current_user %>
      <span class="nav-text"><%= current_user.nickname %></span>
      <%= link_to 'Log Out', logout_path, method: :delete, class: 'nav-link' %>
    <% else %>
      <div class="dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarLogin" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Login
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarLogin">
          <%= link_to 'Github', '/auth/github', class: 'dropdown-item' %>
        </div>
      </div>
    <% end %>
  </div>
</nav>
